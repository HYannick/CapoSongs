import stringify from "@/libs/qs/stringify";
import parse from "@/libs/qs/parse";
import formats from "@/libs/qs/formats";

export default {
  formats: formats,
  parse: parse,
  stringify: stringify,
};
